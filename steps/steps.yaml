check if browser supports the geoloaction

st options for height accuracy , a 5 second timeout , and no caching.

use watchposition to track the user's location continuously.

Emit the latitude and longitude via socket with "send-location",log any errors to the console 

Initialize a map centred at coordinates (0,0) with a zoom level of anything you want to set using leaflet.

Add openStreetmap tiles to map 

create an empty object markers

when receiving the  location data  via the socket ,extract the id ,latitude ,longitude and center the mapon the view coordintes

If a marker for the id exist update the location otherwise  create a new marker at the given coorindates and add it to the map.when a user disconnects ,remove their marker from the map and delete it from the markers